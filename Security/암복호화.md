# 암복호화 (Encryption & Decryption)

## 개념

### 암호화 (Encryption)
평문(원본 데이터)을 **암호문**으로 변환하여 보호

### 복호화 (Decryption)
암호문을 **평문**으로 복원
```
평문 → [암호화] → 암호문 → [복호화] → 평문
Hello      🔒      X7#a2$      🔓      Hello
```

## 암호화 종류

### 1. 단방향 암호화 (Hash)

**특징**: 암호화만 가능, **복호화 불가능**
```
password123 → [Hash] → 5f4dcc3b5aa765d61d8327deb882cf99
                         (복호화 불가 ❌)
```

**용도**:
- 비밀번호 저장
- 데이터 무결성 검증
- 디지털 서명

**주요 알고리즘**:

| 알고리즘 | 출력 길이 | 보안 수준 | 용도 |
|---------|----------|----------|------|
| MD5 | 128bit | 취약 ❌ | 체크섬 |
| SHA-1 | 160bit | 취약 ❌ | 레거시 |
| SHA-256 | 256bit | 안전 ✅ | 일반 용도 |
| SHA-512 | 512bit | 안전 ✅ | 고보안 |
| bcrypt | 가변 | 안전 ✅ | 비밀번호 |

**예시 (Java)**:
```java
import java.security.MessageDigest;

// SHA-256 해시
String password = "password123";
MessageDigest md = MessageDigest.getInstance("SHA-256");
byte[] hash = md.digest(password.getBytes());
// 출력: 5f4dcc3b5aa765d61d8327deb882cf99
```

**비밀번호 저장 (bcrypt)**:
```java
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

// 암호화
String rawPassword = "password123";
String hashedPassword = encoder.encode(rawPassword);
// $2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy

// 검증
boolean matches = encoder.matches(rawPassword, hashedPassword);
// true
```

### 2. 양방향 암호화

#### A. 대칭키 암호화 (Symmetric)

**특징**: **같은 키**로 암호화/복호화
```
평문 + 비밀키 → [암호화] → 암호문
암호문 + 비밀키 → [복호화] → 평문
```

**장점**:
- 빠른 처리 속도
- 낮은 계산 비용

**단점**:
- 키 공유 문제
- 키 관리 어려움

**주요 알고리즘**:

| 알고리즘 | 키 길이 | 블록 크기 | 보안 수준 |
|---------|---------|----------|-----------|
| DES | 56bit | 64bit | 취약 ❌ |
| 3DES | 168bit | 64bit | 약함 ⚠️ |
| AES-128 | 128bit | 128bit | 안전 ✅ |
| AES-256 | 256bit | 128bit | 매우 안전 ⭐ |

**예시 (AES)**:
```java
import javax.crypto.Cipher;
import javax.crypto.KeyGenerator;
import javax.crypto.SecretKey;
import javax.crypto.spec.SecretKeySpec;
import java.util.Base64;

// 키 생성
KeyGenerator keyGen = KeyGenerator.getInstance("AES");
keyGen.init(256);
SecretKey secretKey = keyGen.generateKey();

// 암호화
Cipher cipher = Cipher.getInstance("AES");
cipher.init(Cipher.ENCRYPT_MODE, secretKey);
byte[] encrypted = cipher.doFinal("Hello".getBytes());
String encryptedText = Base64.getEncoder().encodeToString(encrypted);

// 복호화
cipher.init(Cipher.DECRYPT_MODE, secretKey);
byte[] decrypted = cipher.doFinal(Base64.getDecoder().decode(encryptedText));
String decryptedText = new String(decrypted);
// "Hello"
```

#### B. 비대칭키 암호화 (Asymmetric)

**특징**: **다른 키**로 암호화/복호화 (공개키/개인키 쌍)
```
평문 + 공개키 → [암호화] → 암호문
암호문 + 개인키 → [복호화] → 평문
```

**키 쌍**:
- **공개키 (Public Key)**: 누구나 사용 가능
- **개인키 (Private Key)**: 소유자만 보관

**장점**:
- 키 공유 불필요
- 디지털 서명 가능

**단점**:
- 느린 처리 속도
- 높은 계산 비용

**주요 알고리즘**:

| 알고리즘 | 키 길이 | 용도 |
|---------|---------|------|
| RSA | 2048~4096bit | 일반 암호화, 서명 |
| ECC | 256bit | 모바일, IoT |
| DSA | 1024~3072bit | 디지털 서명 전용 |

**예시 (RSA)**:
```java
import java.security.*;
import javax.crypto.Cipher;
import java.util.Base64;

// 키 쌍 생성
KeyPairGenerator keyGen = KeyPairGenerator.getInstance("RSA");
keyGen.initialize(2048);
KeyPair keyPair = keyGen.generateKeyPair();
PublicKey publicKey = keyPair.getPublic();
PrivateKey privateKey = keyPair.getPrivate();

// 암호화 (공개키 사용)
Cipher cipher = Cipher.getInstance("RSA");
cipher.init(Cipher.ENCRYPT_MODE, publicKey);
byte[] encrypted = cipher.doFinal("Hello".getBytes());

// 복호화 (개인키 사용)
cipher.init(Cipher.DECRYPT_MODE, privateKey);
byte[] decrypted = cipher.doFinal(encrypted);
String decryptedText = new String(decrypted);
// "Hello"
```

## 암호화 방식 비교

| 구분 | 단방향 (Hash) | 대칭키 | 비대칭키 |
|------|--------------|--------|---------|
| 복호화 | 불가능 ❌ | 가능 ✅ | 가능 ✅ |
| 키 개수 | 없음 | 1개 | 2개 (공개키/개인키) |
| 속도 | 빠름 ⭐⭐⭐ | 빠름 ⭐⭐ | 느림 ⭐ |
| 용도 | 비밀번호, 무결성 | 데이터 암호화 | 키 교환, 서명 |
| 예시 | SHA-256, bcrypt | AES, DES | RSA, ECC |

## 하이브리드 암호화

**개념**: 대칭키 + 비대칭키 조합

**과정**:
1. 대칭키로 데이터 암호화 (빠름)
2. 비대칭키(공개키)로 대칭키 암호화 (안전)
3. 암호화된 데이터 + 암호화된 키 전송
4. 비대칭키(개인키)로 대칭키 복호화
5. 대칭키로 데이터 복호화

**사용 예**: HTTPS, TLS/SSL
```
[송신자]
데이터 ─(AES)→ 암호문
대칭키 ─(RSA 공개키)→ 암호화된 키

[수신자]
암호화된 키 ─(RSA 개인키)→ 대칭키
암호문 ─(AES + 대칭키)→ 데이터
```

## 응용 분야

### 1. 비밀번호 저장
```java
// bcrypt 사용 (단방향)
BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();
String hashed = encoder.encode("password123");
// DB에 해시값 저장
```

### 2. 데이터베이스 암호화
```java
// AES 사용 (대칭키)
String ssn = "123456-1234567";
String encrypted = AESUtil.encrypt(ssn, secretKey);
// DB에 암호문 저장
```

### 3. HTTPS 통신
```
클라이언트 ←─(RSA)─→ 서버: 대칭키 교환
클라이언트 ←─(AES)─→ 서버: 데이터 암호화 통신
```

### 4. 이메일 암호화 (PGP)
```
평문 이메일 ─(RSA 공개키)→ 암호문
암호문 ─(RSA 개인키)→ 평문 이메일
```

### 5. 파일 암호화
```java
// AES로 파일 암호화
FileInputStream fis = new FileInputStream("file.txt");
Cipher cipher = Cipher.getInstance("AES");
cipher.init(Cipher.ENCRYPT_MODE, secretKey);
CipherOutputStream cos = new CipherOutputStream(fos, cipher);
```

## 보안 모범 사례

### 1. 알고리즘 선택
- ❌ MD5, SHA-1, DES (취약)
- ✅ SHA-256, AES-256, RSA-2048

### 2. 비밀번호 해싱
```java
// ❌ 단순 해시
String hash = SHA256(password);

// ✅ Salt + 반복
String hash = bcrypt(password, salt, rounds);
```

### 3. 키 관리
- 하드코딩 금지
- 환경 변수 또는 키 관리 시스템 사용
- 주기적 키 교체

### 4. 전송 중 암호화
```java
// ✅ HTTPS 사용
https://example.com

// ❌ HTTP 사용
http://example.com
```

## 참고 자료

- [NIST 암호화 표준](https://csrc.nist.gov/projects/cryptographic-standards-and-guidelines)
- [Java Cryptography Architecture](https://docs.oracle.com/javase/8/docs/technotes/guides/security/crypto/CryptoSpec.html)
- [OWASP Cryptographic Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html)